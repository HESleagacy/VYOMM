services:
  vyomm-api:
    image: vyomm-api:local
    ports: ["8080:8080"]
  prometheus:
    image: prom/prometheus:v2.55.1
    volumes: ["../prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro"]
    ports: ["9090:9090"]
